from Bio import SeqIO
from Bio.SeqUtils import gc_fraction

rec = SeqIO.read("unknown_sequence.fasta", "fasta")
seq = rec.seq

print(f"ID: {rec.id}")
print(f"Length: {len(seq)}")
print(f"GC%: {gc_fraction(seq)*100:.2f}")
print(f"Reverse complement (first 50): {seq.reverse_complement()[:50]}")
